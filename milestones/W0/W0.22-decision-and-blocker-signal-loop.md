---
id: W0.22
title: "decision and blocker signal loop"
wave: W0
kind: step
owner: "human (Todd) + agent"
status: in_progress
detail: briefed
depends_on: []
decision_refs: []
success_state: >-
  Loop instance live on its own Supabase project: migrations applied, RLS
  probes green (ci:loop-probe), ledger-ingest receiving on push with
  snapshot events recorded, loop.html rendering front/queue/blockers from
  live data with honest degrade, decision-record-draft producing valid
  patches; no write path from loop to repository exists; D6 posture
  documented as extended-de-facto, not decided.
verification: "ci:loop-probe + human:organizers (loop reviewed live)"
---

# W0.22 · decision and blocker signal loop

The sense/compare/act/record loop described in the spec at
`/workspace/group/attachments/remaining-work-and-signal-loop-spec-v0.1.md`.

Runs on a separate Supabase project (`dgcoffcanzwsuwgpocbt`, project name
"Common Information System") — the separation is structural: the regulator
operationally distinct from the regulated.

`depends_on` is honestly empty because the loop touches no main-project
substrate. Its separateness is what makes the empty edge true.

## Relations to other entries

- **W0.18** (ledger status mirror): `ledger-ingest` is W0.18's mirror, early.
  When W0.18 executes formally it may point here or at a W0.3-successor.
- **W0.21** (decision-coherence validation): the `decision_refs` fix in
  `validate.py` (include per node) will upgrade the loop to v2 schema,
  enabling full leverage computation.
- **D4** (email provider): digest/notification rail is out of scope v0.1.
- **D6** (alpha access posture): public-read RLS posture extends de-facto
  practice; does not decide D6.

## What the loop cannot do

It cannot flip a decision, write to the repository, or mark anything
`recorded` without a URL pointing into the canonical record. Authority stays
exactly where `AGENTS.md` rule 5 puts it.
